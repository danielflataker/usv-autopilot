# datasets/README.md

Small, handpicked datasets used to test parsers/plots and reproduce analyses.

Guidelines:
- Keep files small (a few MB).
- Add a short note per dataset: where it came from + what it's for.
- Raw/full logs belong in [`test_runs/`](../test_runs/).
