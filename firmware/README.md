# firmware/README.md

STM32 firmware (CubeIDE/CubeMX project + application code).

Notes:
- Keep architecture/contracts in [`docs/`](../docs/).
- Shared on-wire formats live in [`docs/comms/`](../docs/comms/).
